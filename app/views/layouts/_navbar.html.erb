<div class="navbar-wagon">
  <!-- Logo -->
  <a href="/" class="navbar-wagon-brand">
    <%= image_tag "cooking.svg", class: "" %>
  </a>

  <% unless params[:controller] == 'pages' && params[:action] == 'home' %>
    <%= form_tag(food_tours_path, method: :get, :class => 'form-inline') do %>
      <%= text_field_tag :q, nil, class: 'form-control nav-search-style' %>
      <%= submit_tag 'Search', class: 'btn btn-primary' %>
    <% end %>
  <% end %>
  <!-- Right Navigation -->
  <div class="navbar-wagon-right hidden-xs hidden-sm">
    <% if user_signed_in? %>
      <a href="" class="navbar-wagon-item navbar-wagon-link">Host a tour</a>
      <a href="" class="navbar-wagon-item navbar-wagon-link">Trips</a>
      <a href="" class="navbar-wagon-item navbar-wagon-link">Messages</a>
    <!-- Profile picture with dropdown list -->
    <div class="navbar-wagon-item">
      <div class="dropdown">

        <%= image_tag "user.png", class: "avatar dropdown-toggle", id: "navbar-wagon-menu", data: { toggle: 'dropdown' } %>

        <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
          <li><a href="#">Profile</a></li>
          <li><a href="#">Dashboard</a></li>
          <li><a href="/food_tours/new">Add a foodtour</a></li>
          <%= link_to "Log out", destroy_user_session_path, method: :delete, class: 'log-out' %>
        </ul>
      </div>
    </div>
    <% else %>
      <%= link_to "Sign In", new_user_session_path, class: 'btn btn-primary sign-in' %>
      <%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-primary sign-up' %>
    <% end %>
  </div>

  <!-- Dropdown appearing on mobile only -->
  <div class="navbar-wagon-item hidden-md hidden-lg">
    <div class="dropdown">
      <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
      <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
        <li><a href="#">Host</a></li>
        <li><a href="#">Trips</a></li>
        <li><a href="#">Messagese</a></li>
      </ul>
    </div>
  </div>
</div>

